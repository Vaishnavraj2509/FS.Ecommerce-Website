# Application name
spring.application.name=Ecommerce

# Server port
server.port=5454

# Database connection with auto-create database
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/ecommerce?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=admin
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate will auto-detect MySQL dialect - no need to specify explicitly
# Automatically update the schema (use cautiously in production)
spring.jpa.hibernate.ddl-auto=update

# Show SQL in logs (for debugging)
spring.jpa.show-sql=true

# Disable open-in-view warning
spring.jpa.open-in-view=false

#PAYMENT GATEWAY
razorpay.api.key = rzp_test_RFUUWlDXjvgeNk 
razorpay.api.secret = RFUAzvKYfcjYB4


